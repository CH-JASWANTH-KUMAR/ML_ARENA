import Hero from '@/components/Hero';

export default function Home() {
  return (
    <main className="min-h-screen">
      <Hero />
      
      {/* Additional sections can be added below the hero */}
      {/* For now, the hero is the focal point */}
    </main>
  );
}
